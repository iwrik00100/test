[local]
localhost ansible_connection=local

[dev]
# Add remote dev servers here if needed
# dev-server ansible_host=192.168.1.100 ansible_user=admin

[prod]
# Add production servers here if self-hosting
# prod-server ansible_host=10.0.0.10 ansible_user=deploy
